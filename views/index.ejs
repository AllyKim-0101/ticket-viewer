<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= title %></title>
  </head>

  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% if(!error){%>
    <p>
      There are <span id="numberOfTicket"><%= numberOfTicket %></span> tickets
      available
    </p>
    <ul>
      <%# this is a comment: loop over each ticket to grab id and subject in
      ticket %> <% for(const ticket of list){%>
      <li>
        <a href="/ticketDetails/<%=ticket.id %>"
          >ID:<%= ticket.id %>, Subject:<%= ticket.subject %></a
        >
      </li>
      <%}%>
    </ul>
    <div id="pagination">
      <% if(previousPage){%>
      <a href="?page=<%= previousPage %>">Previous</a>
      <%}%> <% if (nextPage){%>
      <a href="?page=<%= nextPage %>">Next</a>
      <%}%>
    </div>

    <%}else{ %>

    <span> <%=error %> </span>
    <%}%>
  </body>
</html>
